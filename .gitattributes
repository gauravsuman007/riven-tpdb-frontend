# Keep every file's line endings exactly as committed, so nothing
# renormalises a whole file and turns a small change into a conflict on the
# next upstream merge.
* -text

# Generated by openapi-typescript from the backend's /openapi.json. Never
# hand-merge it: on any conflict, take either side and regenerate.
src/lib/providers/riven.ts linguist-generated=true
